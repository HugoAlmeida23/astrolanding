<!DOCTYPE html>
<html lang="pt">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>FlowTask - Transforme o Caos da Gestão em Lucro e Tranquilidade</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap');

        :root {
            --primary: #8b5cf6;
            --primary-dark: #7c3aed;
            --secondary: #06b6d4;
            --success: #10b981;
            --dark: #0f0f23;
            --dark-lighter: #1a1a2e;
            --dark-card: #16213e;
            --text-primary: #e2e8f0;
            --text-secondary: #94a3b8;
            --gradient-purple: linear-gradient(135deg, #8b5cf6 0%, #7c3aed 100%);
        }

        * { margin: 0; padding: 0; box-sizing: border-box; scroll-behavior: smooth; }

        body {
            font-family: 'Inter', sans-serif;
            background: var(--dark);
            color: var(--text-primary);
            line-height: 1.6;
            overflow-x: hidden;
            font-size: 16px;
        }

        .bg-animation {
            position: fixed; top: 0; left: 0; width: 100%; height: 100%; z-index: -1;
            background: radial-gradient(ellipse at top, rgba(139, 92, 246, 0.1) 0%, transparent 70%),
                        radial-gradient(ellipse at bottom, rgba(6, 182, 212, 0.1) 0%, transparent 70%);
        }

        .container { max-width: 1120px; margin: 0 auto; padding: 0 1.5rem; }

        /* --- Header --- */
        header {
            position: fixed; top: 0; width: 100%; padding: 1rem 0;
            backdrop-filter: blur(20px); -webkit-backdrop-filter: blur(20px);
            border-bottom: 1px solid rgba(139, 92, 246, 0.1); z-index: 1000;
            transition: background 0.3s ease;
        }

        header.scrolled { background: rgba(15, 15, 35, 0.9); }

        nav { display: flex; justify-content: space-between; align-items: center; }

        .logo { font-size: 1.75rem; font-weight: 700; color: white; }
        .logo span { background: var(--gradient-purple); -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text; }

        .nav-links { display: flex; list-style: none; gap: 2rem; }
        .nav-links a { color: var(--text-secondary); text-decoration: none; font-weight: 500; transition: color 0.3s ease; }
        .nav-links a:hover { color: var(--primary); }

        .cta-button {
            background: var(--gradient-purple); color: white; padding: 0.75rem 1.5rem;
            border: none; border-radius: 50px; font-weight: 600; text-decoration: none;
            transition: all 0.3s ease; box-shadow: 0 4px 15px rgba(139, 92, 246, 0.2);
        }
        .cta-button:hover { transform: translateY(-3px); box-shadow: 0 8px 25px rgba(139, 92, 246, 0.3); }
        .mobile-menu-toggle { display: none; }

        /* --- Hero Section --- */
        .hero { padding: 10rem 0 6rem 0; text-align: center; }
        .hero h1 {
            font-size: clamp(2.5rem, 5vw, 4rem); font-weight: 800; line-height: 1.2;
            color: white; margin-bottom: 1.5rem; max-width: 800px; margin-left: auto; margin-right: auto;
        }
        .hero h1 .highlight { color: var(--primary); }
        .hero p {
            font-size: 1.25rem; color: var(--text-secondary); max-width: 700px;
            margin: 0 auto 2.5rem auto;
        }
        .hero .cta-button { font-size: 1.1rem; padding: 1rem 2.5rem; }
        .hero-demo-visual {
            margin-top: 4rem; position: relative;
        }
        .hero-demo-visual img {
            width: 100%; max-width: 800px; border-radius: 12px;
            border: 1px solid rgba(139, 92, 246, 0.2);
            box-shadow: 0 20px 50px rgba(0, 0, 0, 0.4);
        }

        /* --- Social Proof --- */
        .social-proof { padding: 2rem 0; text-align: center; }
        .social-proof p { font-size: 1rem; font-weight: 600; color: var(--text-secondary); margin-bottom: 2rem; }
        .logos { display: flex; justify-content: center; align-items: center; gap: 4rem; flex-wrap: wrap; filter: grayscale(1) opacity(0.6); }
        .logos img { height: 30px; }

        /* --- Section Header --- */
        .section-header { text-align: center; margin-bottom: 4rem; }
        .section-header .tag {
            display: inline-block; background: rgba(139, 92, 246, 0.1); color: var(--primary);
            padding: 0.5rem 1rem; border-radius: 50px; font-weight: 600; margin-bottom: 1rem;
        }
        .section-header h2 { font-size: clamp(2rem, 4vw, 3rem); font-weight: 700; color: white; margin-bottom: 1rem; max-width: 650px; margin-left: auto; margin-right: auto;}
        .section-header p { font-size: 1.1rem; color: var(--text-secondary); max-width: 600px; margin: 0 auto; }

        /* --- How It Works Section --- */
        .how-it-works { padding: 6rem 0; }
        .how-it-works-grid {
            display: grid; grid-template-columns: repeat(3, 1fr); gap: 2rem; text-align: center;
        }
        .step { background: var(--dark-card); padding: 2.5rem 2rem; border-radius: 16px; border: 1px solid rgba(139, 92, 246, 0.1); }
        .step .step-number {
            font-size: 1.5rem; font-weight: 700; display: inline-block;
            background: var(--gradient-purple); color: white;
            width: 50px; height: 50px; line-height: 50px; border-radius: 50%; margin-bottom: 1.5rem;
        }
        .step h3 { font-size: 1.25rem; color: white; margin-bottom: 0.5rem; }
        .step p { color: var(--text-secondary); }

        /* --- Benefits Section (Replaces Features) --- */
        .benefits { padding: 6rem 0; background: rgba(139, 92, 246, 0.05); }
        .benefits-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 2rem; }
        .benefit-card {
            background: var(--dark); padding: 2rem; border-radius: 16px;
            border: 1px solid rgba(139, 92, 246, 0.2); transition: all 0.3s ease;
        }
        .benefit-card:hover { transform: translateY(-5px); border-color: var(--primary); }
        .benefit-icon { font-size: 2.5rem; margin-bottom: 1.5rem; }
        .benefit-card h3 { font-size: 1.25rem; color: white; margin-bottom: 1rem; }
        .benefit-card p { color: var(--text-secondary); }

        /* --- Testimonial Section --- */
        .testimonial { padding: 6rem 0; }
        .testimonial-card {
            background: var(--dark-card); border-radius: 16px; padding: 3rem;
            border: 1px solid rgba(139, 92, 246, 0.2); text-align: center; max-width: 800px; margin: 0 auto;
        }
        .testimonial-text { font-size: 1.5rem; font-style: italic; color: white; line-height: 1.5; margin-bottom: 2rem; }
        .testimonial-author { font-weight: 600; color: var(--text-primary); }
        .testimonial-author span { display: block; font-weight: 400; color: var(--text-secondary); font-size: 0.9rem; }

        /* --- Pricing Section --- */
        .pricing { padding: 6rem 0; }
        .pricing-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); gap: 2rem; align-items: center; }
        .pricing-card {
            background: var(--dark-card); border: 1px solid rgba(139, 92, 246, 0.1); border-radius: 16px;
            padding: 2rem; text-align: center; transition: all 0.3s ease;
        }
        .pricing-card.featured {
            border-color: var(--primary); transform: scale(1.05);
            background: linear-gradient(rgba(15, 15, 35, 0.5), rgba(15, 15, 35, 0.5)),
                        radial-gradient(circle at top, rgba(139, 92, 246, 0.2), transparent 70%);
        }
        .pricing-card h3 { font-size: 1.5rem; font-weight: 600; margin-bottom: 1rem; color: white; }
        .price { font-size: 3rem; font-weight: 800; color: white; margin-bottom: 0.5rem; }
        .price .period { font-size: 1rem; font-weight: 400; color: var(--text-secondary); }
        .pricing-features { list-style: none; text-align: left; margin: 2rem 0; }
        .pricing-features li { display: flex; gap: 0.75rem; margin-bottom: 1rem; color: var(--text-secondary); align-items: center;}
        .pricing-features li::before { content: '✓'; color: var(--success); font-weight: bold; }
        .pricing-card .cta-button { width: 100%; }
        .pricing-card .btn-secondary { background: rgba(139, 92, 246, 0.1); color: var(--primary); border: 1px solid var(--primary); }

        /* --- FAQ Section --- */
        .faq { padding: 6rem 0; }
        .faq-grid { max-width: 800px; margin: 0 auto; display: flex; flex-direction: column; gap: 1rem; }
        .faq-item { background: var(--dark-card); border-radius: 12px; border: 1px solid rgba(139, 92, 246, 0.1); }
        .faq-question {
            width: 100%; text-align: left; background: none; border: none;
            padding: 1.5rem; font-size: 1.1rem; font-weight: 600; color: white;
            cursor: pointer; display: flex; justify-content: space-between; align-items: center;
        }
        .faq-question::after { content: '+'; font-size: 1.5rem; transition: transform 0.3s ease; }
        .faq-answer {
            max-height: 0; overflow: hidden; transition: max-height 0.3s ease, padding 0.3s ease;
            color: var(--text-secondary); line-height: 1.7;
        }
        .faq-answer p { padding: 0 1.5rem 1.5rem 1.5rem; }
        .faq-item.active .faq-question::after { transform: rotate(45deg); }
        .faq-item.active .faq-answer { max-height: 300px; }


        /* --- Final CTA Section --- */
        .final-cta { padding: 6rem 0; text-align: center; }
        .final-cta h2 { font-size: clamp(2rem, 4vw, 3rem); font-weight: 700; color: white; margin-bottom: 1rem; max-width: 700px; margin: 0 auto 1.5rem auto;}
        .final-cta p { font-size: 1.1rem; color: var(--text-secondary); max-width: 600px; margin: 0 auto 2.5rem auto; }
        .final-cta .cta-button { font-size: 1.1rem; padding: 1rem 2.5rem; }

        /* --- Footer --- */
        footer { padding: 4rem 0 2rem 0; background: var(--dark-lighter); border-top: 1px solid rgba(139, 92, 246, 0.2); }
        .footer-bottom { text-align: center; padding-top: 2rem; border-top: 1px solid rgba(139, 92, 246, 0.1); color: var(--text-secondary); font-size: 0.9rem; }
        
        /* --- Responsive Design --- */
        @media (max-width: 992px) {
            .nav-links { display: none; } /* Basic mobile nav, would need JS for a hamburger menu */
            .mobile-menu-toggle { display: block; }
        }
        @media (max-width: 768px) {
            .how-it-works-grid { grid-template-columns: 1fr; }
            .pricing-grid { grid-template-columns: 1fr; }
            .pricing-card.featured { transform: scale(1); }
            .logos { gap: 2rem; }
            .logos img { height: 25px; }
            .hero { padding: 8rem 0 4rem 0; }
        }

    </style>
</head>
<body>
    <div class="bg-animation"></div>

    <header id="header">
        <div class="container">
            <nav>
                <div class="logo">Flow<span>Task</span></div>
                <ul class="nav-links">
                    <li><a href="#benefits">Vantagens</a></li>
                    <li><a href="#how-it-works">Como Funciona</a></li>
                    <li><a href="#pricing">Preços</a></li>
                    <li><a href="#faq">FAQ</a></li>
                </ul>
                <a href="#pricing" class="cta-button">Ver Planos</a>
            </nav>
        </div>
    </header>

    <main>
        <!-- HERO SECTION -->
        <section class="hero">
            <div class="container">
                <h1>Pare de gerir o <span class="highlight">caos</span>. Comece a construir o <span class="highlight">futuro</span>.</h1>
                <p>Cansado de se afogar em folhas de cálculo, emails e prazos fiscais? FlowTask é a plataforma inteligente que automatiza o trabalho administrativo para que possa focar-se no que realmente importa: fazer o seu negócio crescer.</p>
                <a href="#final-cta" class="cta-button">Peça uma Demo Gratuita</a>
                <div class="hero-demo-visual">
                    <!-- Placeholder for a clean, benefit-focused product image or video -->
                    <img src="https://i.imgur.com/8Q1j2oV.png" alt="Dashboard do FlowTask mostrando a rentabilidade e tarefas ativas">
                </div>
            </div>
        </section>

        <!-- SOCIAL PROOF SECTION -->
        <section class="social-proof">
            <div class="container">
                <p>Junte-se às empresas que já transformaram a sua gestão em Portugal</p>
                <div class="logos">
                    <!-- Replace with real client logos -->
                    <img src="https://via.placeholder.com/120x30/FFFFFF/000000?text=Empresa+A" alt="Logo Empresa A">
                    <img src="https://via.placeholder.com/120x30/FFFFFF/000000?text=Contabilidade+X" alt="Logo Contabilidade X">
                    <img src="https://via.placeholder.com/120x30/FFFFFF/000000?text=Consultores+Y" alt="Logo Consultores Y">
                    <img src="https://via.placeholder.com/120x30/FFFFFF/000000?text=Agência+B" alt="Logo Agência B">
                </div>
            </div>
        </section>

        <!-- HOW IT WORKS SECTION -->
        <section class="how-it-works" id="how-it-works">
            <div class="container">
                <div class="section-header">
                    <span class="tag">Simples. Rápido. Poderoso.</span>
                    <h2>Comece a otimizar em 3 passos simples</h2>
                </div>
                <div class="how-it-works-grid">
                    <div class="step">
                        <div class="step-number">1</div>
                        <h3>Conecte os Seus Dados</h3>
                        <p>Importe facilmente os seus clientes, faturas e ficheiros SAFT. A nossa plataforma organiza tudo automaticamente.</p>
                    </div>
                    <div class="step">
                        <div class="step-number">2</div>
                        <h3>Deixe a IA Trabalhar</h3>
                        <p>A nossa Inteligência Artificial analisa, categoriza tarefas, monitoriza prazos fiscais e identifica oportunidades de melhoria.</p>
                    </div>
                    <div class="step">
                        <div class="step-number">3</div>
                        <h3>Tome Decisões Inteligentes</h3>
                        <p>Receba relatórios claros sobre rentabilidade e produtividade. Use o tempo que poupou para focar no crescimento estratégico.</p>
                    </div>
                </div>
            </div>
        </section>

        <!-- BENEFITS SECTION -->
        <section class="benefits" id="benefits">
            <div class="container">
                <div class="section-header">
                    <span class="tag">O Fim da Sobrecarga</span>
                    <h2>Construído para lhe devolver o seu bem mais precioso: o tempo.</h2>
                    <p>Cada funcionalidade foi desenhada para eliminar uma frustração, automatizar uma tarefa repetitiva e dar-lhe a clareza que precisa para ter sucesso.</p>
                </div>
                <div class="benefits-grid">
                    <div class="benefit-card">
                        <div class="benefit-icon">🧠</div>
                        <h3>Receba Respostas, Não Apenas Dados</h3>
                        <p>Em vez de procurar em relatórios complexos, pergunte ao nosso AI Advisor: "Qual o meu cliente menos rentável este mês?". Obtenha respostas instantâneas e tome ações estratégicas.</p>
                    </div>
                    <div class="benefit-card">
                        <div class="benefit-icon">🎯</div>
                        <h3>Durma Tranquilo Durante a Época Fiscal</h3>
                        <p>Nunca mais perca um prazo. FlowTask gere automaticamente as suas obrigações fiscais, alertando-o com antecedência para que tenha total paz de espírito.</p>
                    </div>
                    <div class="benefit-card">
                        <div class="benefit-icon">⏱️</div>
                        <h3>Registe Horas Sem Esforço</h3>
                        <p>Esqueça os cronómetros. Diga ao nosso AI Advisor "Gastei 2 horas no projeto X" e ele regista por si. Faturar horas torna-se simples e preciso.</p>
                    </div>
                    <div class="benefit-card">
                        <div class="benefit-icon">📄</div>
                        <h3>Diga Adeus à Entrada Manual de Faturas</h3>
                        <p>Faça upload de uma fatura e a nossa IA extrai os dados, associa ao cliente correto e prepara tudo para si. Recupere horas do seu dia.</p>
                    </div>
                    <div class="benefit-card">
                        <div class="benefit-icon">📈</div>
                        <h3>Descubra o Lucro Escondido</h3>
                        <p>Saiba exatamente que clientes, projetos e serviços dão mais lucro. Pare de adivinhar e comece a otimizar a sua rentabilidade com dados claros e visuais.</p>
                    </div>
                     <div class="benefit-card">
                        <div class="benefit-icon">👥</div>
                        <h3>Tenha uma Visão 360º dos Seus Clientes</h3>
                        <p>Acesse todo o histórico, tarefas, rentabilidade e documentos de um cliente num único local. Prepare-se para qualquer reunião em segundos.</p>
                    </div>
                </div>
            </div>
        </section>

        <!-- TESTIMONIAL SECTION -->
        <section class="testimonial">
            <div class="container">
                <div class="testimonial-card">
                    <p class="testimonial-text">"FlowTask mudou o jogo para nós. Reduzimos o tempo gasto em tarefas administrativas em mais de 15 horas por semana. Agora, finalmente, temos tempo para focar na estratégia e no atendimento ao cliente."</p>
                    <p class="testimonial-author">Joana Silva<span>CEO, Contabilidade X</span></p>
                </div>
            </div>
        </section>

        <!-- PRICING SECTION -->
        <section class="pricing" id="pricing">
            <div class="container">
                <div class="section-header">
                    <span class="tag">Preços Transparentes</span>
                    <h2>Um plano para cada fase do seu negócio</h2>
                </div>
                <div class="pricing-grid">
                    <div class="pricing-card">
                        <h3>Starter <span style="display:block; font-size:0.9rem; font-weight:500; color: var(--text-secondary);">(Solo)</span></h3>
                        <div class="price">29€ <span class="period">/mês</span></div>
                        <ul class="pricing-features">
                            <li>1 utilizador</li>
                            <li>Até 30 clientes</li>
                            <li>Tarefas, tempos e despesas (básico)</li>
                            <li>Upload SAFT-PT</li>
                            <li>100 leituras de QR de faturas/mês</li>
                            <li>1 GB de armazenamento</li>
                            <li>100 mensagens de IA/mês</li>
                        </ul>
                        <a href="#final-cta" class="cta-button btn-secondary">Peça uma Demo</a>
                    </div>
                    <div class="pricing-card featured">
                        <h3>Team</h3>
                        <div class="price">79€ <span class="period">/mês</span></div>
                        <ul class="pricing-features">
                            <li>Até 5 utilizadores</li>
                            <li>200 clientes</li>
                            <li>Workflows e aprovações</li>
                            <li>Dashboards de rentabilidade</li>
                            <li>1 000 leituras de QR/mês</li>
                            <li>10 GB de armazenamento</li>
                            <li>1 000 mensagens de IA/mês</li>
                            <li>Relatórios agendados</li>
                            <li>Digests por email</li>
                        </ul>
                        <a href="#final-cta" class="cta-button">Peça uma Demo</a>
                    </div>
                    <div class="pricing-card">
                        <h3>Business</h3>
                        <div class="price">199€ <span class="period">/mês</span></div>
                        <ul class="pricing-features">
                            <li>Até 20 utilizadores</li>
                            <li>Clientes ilimitados</li>
                            <li>Analítica avançada</li>
                            <li>Regras de escalamento</li>
                            <li>Prioridade de processamento (Celery)</li>
                            <li>5 000 leituras de QR/mês</li>
                            <li>50 GB de armazenamento</li>
                            <li>5 000 mensagens de IA/mês</li>
                            <li>SSO-ready</li>
                            <li>Limites de API superiores</li>
                            <li>Automação de relatórios</li>
                        </ul>
                        <a href="#final-cta" class="cta-button btn-secondary">Peça uma Demo</a>
                    </div>
                    <div class="pricing-card">
                        <h3>Add-ons</h3>
                        <ul class="pricing-features">
                            <li>Utilizador extra: 10€/utilizador</li>
                            <li>Armazenamento extra: 5€ por 20 GB</li>
                            <li>QR scans extra: 10€ por 1 000</li>
                            <li>IA extra: 20€ por 1 000 mensagens (ou metered por tokens)</li>
                            <li>Suporte Premium/SLA: 99€</li>
                        </ul>
                        <a href="#final-cta" class="cta-button btn-secondary">Falar com Vendas</a>
                    </div>
                </div>
            </div>
        </section>

        <!-- FAQ SECTION -->
        <section class="faq" id="faq">
            <div class="container">
                 <div class="section-header">
                    <span class="tag">Dúvidas?</span>
                    <h2>Respostas às suas perguntas</h2>
                </div>
                <div class="faq-grid">
                    <div class="faq-item">
                        <button class="faq-question">O FlowTask é seguro para os dados da minha empresa?</button>
                        <div class="faq-answer">
                            <p>Absolutamente. A segurança é a nossa prioridade máxima. Usamos encriptação de ponta (a mesma dos bancos) e servidores seguros na Europa. Os seus dados estão protegidos com as mais altas normas de segurança.</p>
                        </div>
                    </div>
                    <div class="faq-item">
                        <button class="faq-question">A implementação é complicada? Quanto tempo demora?</button>
                        <div class="faq-answer">
                            <p>Não. Desenhámos o FlowTask para ser intuitivo. A maioria dos nossos clientes está a funcionar em menos de uma hora. A nossa equipa de suporte também está disponível para o ajudar em todo o processo de importação e configuração inicial.</p>
                        </div>
                    </div>
                    <div class="faq-item">
                        <button class="faq-question">Como funciona exatamente o "AI Advisor"? Parece complicado.</button>
                        <div class="faq-answer">
                            <p>Pense nele como um assistente super inteligente. Ele analisa os dados que já existem na plataforma (horas, faturas, tarefas) para encontrar padrões. Não precisa de configurar nada de complexo. Basta fazer perguntas em linguagem normal e ele responde, tal como faria a um consultor.</p>
                        </div>
                    </div>
                    <div class="faq-item">
                        <button class="faq-question">Este software está em conformidade com a Autoridade Tributária (AT) em Portugal?</button>
                        <div class="faq-answer">
                            <p>Sim. O nosso sistema foi construído com as especificidades do mercado português em mente, incluindo a gestão de obrigações fiscais e o processamento de ficheiros SAFT, para garantir que está sempre em conformidade.</p>
                        </div>
                    </div>
                    <div class="faq-item">
                        <button class="faq-question">E se eu não gostar? Existe algum contrato de fidelização?</button>
                        <div class="faq-answer">
                            <p>Não existem contratos de fidelização. Pode cancelar a sua subscrição a qualquer momento, sem perguntas. A nossa confiança está no valor que o produto lhe trará todos os dias.</p>
                        </div>
                    </div>
                </div>
            </div>
        </section>

        <!-- FINAL CTA -->
        <section class="final-cta" id="final-cta">
             <div class="container">
                <h2>Pronto para trocar o stress pela estratégia?</h2>
                <p>Veja em 15 minutos como o FlowTask pode transformar a sua operação. Agende uma demonstração personalizada e gratuita com um dos nossos especialistas.</p>
                <!-- This would ideally link to a Calendly or similar scheduling tool -->
                <a href="#" class="cta-button">Agendar a Minha Demo Gratuita</a>
            </div>
        </section>

    </main>

    <footer>
        <div class="container">
            <div class="footer-bottom">
                 <p>© 2025 FlowTask. Todos os direitos reservados.</p>
            </div>
        </div>
    </footer>

    <script>
        // Simple script for header scroll and FAQ accordion
        document.addEventListener('DOMContentLoaded', () => {
            // Header scroll effect
            const header = document.getElementById('header');
            window.addEventListener('scroll', () => {
                if (window.scrollY > 50) {
                    header.classList.add('scrolled');
                } else {
                    header.classList.remove('scrolled');
                }
            });

            // FAQ Accordion
            const faqItems = document.querySelectorAll('.faq-item');
            faqItems.forEach(item => {
                const question = item.querySelector('.faq-question');
                question.addEventListener('click', () => {
                    const currentlyActive = document.querySelector('.faq-item.active');
                    if (currentlyActive && currentlyActive !== item) {
                        currentlyActive.classList.remove('active');
                    }
                    item.classList.toggle('active');
                });
            });
        });
    </script>
</body>
</html>